name: dockerbuild-and-push

on:
  push:
    branches:
      - "master"
       
jobs:
  build-container:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Docker Login
        env:
          DOCKER_USER: ${{ secrets.DOCKER_USER }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_TOKEN }}
        run: |
          docker login -u $DOCKER_USER -p $DOCKER_PASSWORD

      - name: Build image
        run: docker build . --file Dockerfile --tag amaraldavi/meu-custom-nginx:$(date +%s)


      - name: Push image
        run: docker push ${{ secrets.DOCKER_USER }}/meu-custom-nginx:$(date +%s)

